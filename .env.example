# ==============================================================================
# HemaCare OS — PostgreSQL Backend Environment Template
# Copy this file to .env and configure with your PostgreSQL credentials
# ==============================================================================

PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
